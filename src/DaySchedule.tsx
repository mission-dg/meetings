import {addDays,clockTime,shiftDay,shiftLabel,type SchedulerData,type WorkShift} from './scheduler';
import {assignmentGroup,mealPeriod,sortedAssignments,absencesOnDay} from './scheduleDisplay';
import {displayTime,positionName} from './domain';
export function DaySchedule({data,week,shifts,editable,busy,onEdit,onAdd,showAbsences=false,group='',mine=false}:{data:SchedulerData;week:string;shifts:WorkShift[];editable:boolean;busy:boolean;onEdit:(s:WorkShift)=>void;onAdd:(day:string)=>void;showAbsences?:boolean;group?:string;mine?:boolean}){
 return <div className="day-schedule">{Array.from({length:7},(_,i)=>{const day=addDays(week,i),items=sortedAssignments(shifts.filter(s=>shiftDay(s.start)===day),data.people);const absences=showAbsences&&data.self.is_manager?absencesOnDay(data.requests,day).filter(q=>{const p=data.people.find(p=>p.id===q.person_id);return (!mine||q.person_id===data.self.person_id)&&(!group||p?.group===group)}):[];return <section className="schedule-day" key={day}><header><h3>{['Sunday','Monday','Tuesday','Wednesday','Thursday','Friday','Saturday'][i]}</h3><time dateTime={day}>{day.slice(5)}</time></header>{(data.self.is_manager?['Lunch','Dinner']:['All']).map(period=><section className={'meal-section '+period.toLowerCase()} key={period}>{period!=='All'&&<h4>{period}</h4>}{['FOH','BOH','Catering','SHL'].map(g=>{const list=items.filter(s=>(period==='All'||mealPeriod(s)===period)&&assignmentGroup(s,data.jobs)===g);return list.length>0&&<section className="coverage-group" key={g}><h5>{positionName(g)} <span>({new Set(list.map(s=>s.person_id)).size})</span></h5>{list.map(s=>{const content=<><strong>{data.people.find(p=>p.id===s.person_id)?.name||'Former teammate'}</strong><span>{clockTime(s.start)}–{clockTime(s.end)}{shiftDay(s.start)!==shiftDay(s.end)?' +1 day':''}</span><span>{shiftLabel(s,data.jobs)}</span>{data.training.some(t=>t.status==='Scheduled'&&(t.work_shift_id===s.id||t.trainer_shift_id===s.id))&&<small>Training scheduled</small>}</>;return editable?<button key={s.id} className="day-shift draft-shift" disabled={busy} onClick={()=>onEdit(s)}>{content}</button>:<div key={s.id} className="day-shift published-shift">{content}</div>})}</section>})}{!items.some(s=>period==='All'||mealPeriod(s)===period)&&<p className="day-empty">No shifts scheduled.</p>}</section>)}{editable&&<button className="add-shift" disabled={busy} onClick={()=>onAdd(day)} aria-label={'Add shift on '+day}>＋ Add shift</button>}{absences.length>0&&<section className="day-absences"><h4>Approved absences</h4>{absences.map(q=><p className="absence-badge" key={q.id}><strong>{data.people.find(p=>p.id===q.person_id)?.name||'Former teammate'}</strong><br/>{q.payload.category||'Time off'}<br/>{displayTime(q.payload.start!)}–{displayTime(q.payload.end!)}</p>)}</section>}</section>})}</div>
}
